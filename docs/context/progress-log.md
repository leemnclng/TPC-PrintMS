# Progress Log

Chronological notes about app progress.

## 2026-06-06

- Created the initial context documentation structure under `docs/context`.

## 2026-08-15

- Clarified the Printing-MS application boundary and its relationship to the external business workflow.
- Recorded the six initial functional areas and the product questions that must be settled before implementation.
- Next: define roles, core entities, job-order statuses, and MVP acceptance criteria before scaffolding the application.
- Added Canon PIXMA G4770 as the first printer target while preserving vendor-neutral printer support.
- Drafted the phased initial desktop application build plan, starting with cross-platform printing validation.
- Agreed on Electron delivery for Windows and macOS, local-first persistence, and no Docker requirement on customer machines.
- Selected FastAPI as the local backend, with `uv`/`.venv` for development and a bundled Python executable for customer releases.
- Defined the initial application navigation, page contents, nested workspaces, and coverage of every initial Printing-MS capability.

## 2026-08-15 (initial application scaffold)

- Summary: Built the Phase 2 "Application Foundation" scaffold end-to-end and verified it launches for real (Electron window rendering, backend responding, full navigation walked with screenshots) rather than just typechecking.
- Completed:
  - Monorepo scaffold: `apps/web` (React + TS + Vite renderer), `apps/desktop` (Electron main/preload), `services/api` (FastAPI + SQLAlchemy + Alembic backend), root npm workspaces + dev orchestration script.
  - Custom brand-derived design system ("Press" theme) — see decisions.md. Tokens, typography, spacing, motion, and shared UI primitives (Button, StatusPill, Card, DataTable, EmptyState/RegistrationMark, LoadingState, ErrorState, PlannedNotice, PageHeader) all built from scratch against the real logo, not a template.
  - Full core data model (Customer, Product/Variant, Quotation/Item, JobOrder, Payment, JobFile, Printer, PrintJob, StatusEvent, AuditEntry, BusinessProfile) as SQLAlchemy models with an Alembic-autogenerated initial migration.
  - Real (not mocked) FastAPI routers: health, overview aggregation, full CRUD for customers and products, read-only quotations/job-orders (write endpoints return honest 501s pending Phase 3/4 business rules), business-profile settings, and printer discovery via a CUPS (`lpstat`) adapter — a Windows adapter is stubbed but not implemented.
  - Electron main process spawns and owns the FastAPI backend lifecycle (`uv run`), hands the renderer a resolved loopback URL + per-launch token via a narrow preload bridge, and shuts the backend down cleanly on quit.
  - All 9 primary pages (Overview, Job Orders, Quotations, Production, Print Center, Product Catalog, Customers, Reports, Settings) and the 4 nested workspaces (Job Order, Quotation, Product, Customer) exist with honest loading/empty/error states and explicit "Planned · Phase N" markers on anything not yet functional.
  - Verified: backend starts and serves real requests (`curl` against every route), `tsc`/`vite build` succeed for both the renderer and the Electron process, and a full Electron launch (via a Playwright driver) walked Overview → Print Center → Settings → New Customer with screenshots confirming the design renders as intended and the backend-connection indicator, printer discovery, and business-profile form are genuinely live.
- Next steps: Phase 0 product-baseline decisions blocking Phase 3+ (roles, canonical status transitions, pricing rules, file formats — see issues-log.md), then build quotation/job-order creation on top of the existing read models.
- Two bugs found and fixed during that verification pass (not left as known issues):
  - A startup race where Electron learned the backend's port/token before FastAPI's own startup (migrations + seed) had actually finished, so the renderer's first requests could fail even though the backend came up moments later. Fixed by moving the ready signal to the end of the FastAPI lifespan function (`services/api/app/main.py`) instead of before `uvicorn.run`; `useResource` also gained one silent retry so a similar hiccup doesn't strand a page in a permanent error state.
  - Link-styled buttons (`New product`, `New customer`, etc.) rendered with a browser-default underline because `LinkButton` renders a real `<a>`. Fixed with `text-decoration: none` on `.btn` (`apps/web/src/components/Button/Button.css`).

## 2026-08-15 (run scripts)

- Summary: Added one-command dev launchers so running the app doesn't require knowing the npm workspace commands.
- Completed: `scripts/run.sh` (macOS/Linux) and `scripts/run.bat` (Windows) — both check for Node.js and `uv`, install `uv` automatically if missing (Homebrew on macOS, winget or the official installer on Windows, the official installer script on Linux), run `npm install` on first use, then `npm run dev`. README updated to point to these first.

## 2026-08-16 (sidebar logo mark)

- Summary: Swapped the sidebar's top-left brand icon for a proper transparent-PNG crop of the logo once a cleaner source file (`TPC-Logo.png`, no paper-texture background) was supplied.
- Completed: Cropped it tightly to the wordmark (`apps/web/src/assets/brand/the-paper-club-mark.png`, 256×256, true alpha), swapped `Sidebar.tsx`/`Sidebar.css` to use it with plain `object-fit: contain` instead of the JPEG's zoom/crop/border workaround, and verified via a real Electron launch that it renders cleanly with no background seam.

## 2026-08-16 (sidebar redesign — enterprise nav)

- Summary: Redesigned the sidebar navigation per a direct request to read more like an enterprise management console and to drop the numbered index.
- Completed:
  - New hand-drawn icon set (`AppShell/NavIcons.tsx`) — nine distinct glyphs (dashboard grid, clipboard, document, cycle arrow, printer, box, person, bar chart, sliders), one stroke weight, tailored to this app's actual sections rather than a generic icon-library import.
  - `navItems.ts` restructured into `navGroups` (Sales, Operations, Directory, Insights, plus pinned Overview/Settings) while preserving the exact original item order.
  - `Sidebar.tsx`/`Sidebar.css` rewritten: icon + label nav rows, uppercase group labels, a filled accent-pill active state (replacing the left border bar), and an account-style footer (avatar with an online-status dot + "Owner" + connection text) replacing the bare status dot.
  - Dropped the numbered index everywhere it appeared for navigation wayfinding: the top bar breadcrumb and the "NN · SECTION" prefix on every page/workspace header's eyebrow. `.numeric` (mono tabular figures for job numbers, amounts, timestamps) is unrelated and untouched.
  - Verified via a real Electron launch + screenshots (Overview, Job Orders) that grouping, icons, active/hover states, and the new footer render correctly.
- Next steps: none blocking; this was a scoped visual/component redesign, not a functionality change.

## 2026-08-21 (service catalog hierarchy)

- Summary: Reworked the flat Product Catalog into a Service → Product hierarchy for owner-managed quotation and job-order inputs.
- Completed: Added service CRUD/API/model/migration, migrated existing categories into services, nested product creation/editing under service workspaces, updated navigation and quick actions, and added API coverage for the hierarchy and guarded service deletion.
- Verified: renderer production build and lint pass; API test passes; migration upgrade/downgrade preserves an existing product and category/service association.
- Follow-up: live browser viewport QA could not run because no in-app browser was connected in this session.

## 2026-08-21 (service workspace layout)

- Summary: Simplified service workspaces into product-inventory views and moved service management to a dedicated settings route.
- Completed: Removed the inline settings form from service detail, added clear Settings and New product actions, kept the product table/empty state as the main content, and constrained the separate settings form to a readable width.
- Verified: renderer production build and lint pass; Hallmark post-build audit passes.

## 2026-08-21 (development console noise)

- Summary: Stopped Electron DevTools from opening automatically during normal development startup.
- Completed: Detached DevTools is now opt-in with `PRINTING_MS_OPEN_DEVTOOLS=1`, preventing unsupported Chromium Autofill protocol warnings from appearing on every launch.
- Verified: desktop TypeScript build passes.

## 2026-08-21 (service workspace product request)

- Summary: Fixed the 404 shown when opening a service.
- Completed: Aligned the service workspace product-list request with the backend's existing filtered products endpoint.
- Verified: renderer production build and lint pass.

## 2026-08-21 (UX standard and product creation modal)

- Summary: Added a durable UX development standard and applied it to product creation.
- Completed: Added an accessible shared native-dialog component, moved service-level product creation into a responsive modal, preserved form data on failure, prevented duplicate submission, closed on success, and inserted the created product into the visible inventory without a toast or page change.
- Context: `AGENTS.md` now requires the UX standard for future user-facing development.
- Verified: renderer production build and lint pass; service-catalog API test passes; static Hallmark post-build audit passes. Live browser walkthrough remains unavailable because no browser connection was present in this session.

## 2026-08-21 (inventory operations)

- Summary: Added owner-managed production inventory and connected materials to products and job-order auditing.
- Completed: Added the Operations → Inventory page, responsive material register, low-stock monitoring, modal registration/editing, immutable stock adjustments, product per-unit material recipes, and job-order material movement history.
- Data: Added inventory items, product material requirements, and job-linked inventory movement tables plus FastAPI endpoints and an Alembic migration.
- Verified: API tests pass, the full migration chain upgrades from an empty database, and the renderer production build and lint pass.
- Follow-up: define when the future writable job-order workflow reserves and deducts recipe quantities; tracked in `issues-log.md`.

## 2026-08-21 (required product material recipes)

- Summary: Made material usage part of product creation instead of a follow-up setup step.
- Completed: The New Product modal now requires one or more active inventory materials with a positive per-product quantity, prevents duplicate selections, and directs owners to Inventory when no material is available. The direct product workspace follows the same rule, and the API rejects recipe-less product creation.
- Verified: renderer typecheck, lint, and production build pass; both API test suites pass under the environment’s matching x86 architecture.
- Follow-up: live responsive browser QA was unavailable because no browser connection was present in this session.

## 2026-08-23 (product material multi-select)

- Summary: Reframed product recipes as an owner-managed set of materials that a product is allowed to use, with no product-level consumption quantity.
- Completed: Replaced repeated material dropdown rows with a reusable checkbox multi-select in both product creation and editing. Removed the irrelevant expected-per-product input; each selection now shows identity and stock context only.
- Data: Renamed product material requirements to assignments, removed `quantity_per_unit`, and added a migration that preserves existing product-to-material links. API coverage verifies multiple assignments.
- Verified: renderer typecheck, lint, production build, and both API test suites pass.
- Next steps: When writable job-order creation is implemented, filter its selector to the product’s assigned materials and calculate deduction quantities from page details.

## 2026-08-23 (job-order creation and material usage)

- Summary: Added the first complete owner-driven job-order workflow.
- Completed: Owners can create an order in a responsive modal, choose a customer, add multiple product lines, capture pages/copies/sides, select only materials assigned to each product, and confirm planned quantities. The workspace shows line/material progress and records actual usage through a separate confirmation modal.
- Data: Added job-order items and material plans. Usage updates stock and creates immutable job/product-linked movements in one transaction; order creation itself never deducts stock.
- Verified: API tests pass; renderer typecheck, lint, and production build pass; the new migration upgrades, downgrades, and re-upgrades successfully. Local services launched, but screenshot QA was unavailable because no browser was connected.
- Next steps: Finalize material-specific automatic estimates, accepted-quotation conversion, payments, files, and status transitions.

## 2026-08-23 (optional job-order customer)

- Summary: Removed the customer prerequisite from manual job-order creation.
- Completed: The creation modal defaults to Walk-in / no customer and still allows linking a saved customer. Job-order lists, production cards, and workspaces display a consistent Walk-in fallback.
- Data: Made only the job-order customer foreign key nullable; quotations still require a customer. The rollback migration preserves walk-in orders by linking them to a generated migration customer before restoring the old constraint.
- Verified: API tests, renderer typecheck/lint, and a migration round-trip containing a real customer-less order pass.

## 2026-08-23 (production-first job-order modal)

- Summary: Reordered job-order creation around the fields required to produce the work.
- Completed: Product, pages, copies, print sides, and materials now appear first and receive initial focus. Customer, due date, total, and notes are grouped as secondary order details at the bottom.
- Verified: Renderer typecheck, lint, and production build pass.

## 2026-08-23 (job-order product card pane)

- Summary: Replaced the job-order product dropdown with a scannable product-card selector.
- Completed: Each card shows its service, product name, material-option count, and variant count. Selection remains a keyboard-accessible radio group with responsive layout and visible validation.
- Verified: Renderer typecheck, lint, and production build pass.

## 2026-08-23 (product search in job orders)

- Summary: Added scalable filtering to each job-order product pane.
- Completed: Search matches product names, services, and variant labels; reports the visible result count; provides a clear-search empty state; and keeps the selected product visible.
- Verified: Renderer typecheck, lint, and production build pass.

## 2026-08-23 (compact product search styling)

- Summary: Reduced the space used by product search without removing context.
- Completed: Combined the visible Search label, icon, input, and result count into one control-height row with unified hover/focus treatment and a narrower mobile presentation.
- Verified: Renderer typecheck, lint, and production build pass.

## 2026-08-23 (product and job-order pricing)

- Summary: Added server-calculated Philippine-peso pricing with product variants.
- Completed: Product forms now define a base price per billable page/unit and named adjustments such as Back-to-back, with final-price previews and validation. Job creation previews line/order totals, while the API calculates and snapshots the authoritative unit price and line total.
- Data: Added immutable pricing snapshots to job-order items through an Alembic migration so later catalog edits do not change existing orders.
- Verified: API tests pass, including historical-price preservation; renderer typecheck, lint, and web/desktop production builds pass; migration upgrade/downgrade/re-upgrade reaches head.

## 2026-08-23 (reusable service variants)

- Summary: Moved variant management from free-text product rows into a reusable service-level library.
- Completed: Added a service Variants workspace with focused create/edit flows, active/inactive state, linked-product counts, and guarded removal. Product creation/editing now selects from that library and sets product-specific price adjustments.
- Data: Added service variants and linked product-variant assignments. The migration preserves existing variant names and price adjustments through upgrade, downgrade, and re-upgrade.
- Verified: API tests, renderer typecheck/lint, web/desktop production builds, and the data-preserving migration round trip pass. Live viewport QA was unavailable because no in-app browser was connected.

## 2026-08-23 (global variant library)

- Summary: Promoted reusable variants from individual services into one catalog-wide library.
- Completed: Added the Global variants workspace under the main Services page. Product creation and editing now use the same active variant list across every service while keeping product-specific PHP adjustments.
- Data: Replaced service-owned variants with global records. Migration deduplicates matching names case-insensitively and preserves product assignments and price adjustments.
- Verified: API coverage proves one variant can be assigned to products in different services; typecheck, lint, tests, and migration upgrade/downgrade/re-upgrade pass.

## 2026-08-23 (product print types)

- Summary: Added a required print type to every service product.
- Completed: Product creation and editing now offer only Colored or B&W (Black and white). Service product lists and job-order product cards display and search the selected type.
- Data: Added an enum-constrained `print_type` column; existing products default to B&W and can be corrected in Product Workspace.
- Verified: API tests, typecheck, lint, web/desktop production builds, and migration upgrade/downgrade/re-upgrade pass.

## 2026-08-24 (document analyzer)

- Summary: Added the first production-ready Document Analyzer workspace under Operations.
- Completed: Added local parsers for PDF, image, DOCX, XLSX, and PPTX; normalized print/content metadata; color and B&W page estimation; warnings and confidence; configurable PHP rate rules; secure multipart APIs; and a responsive upload/results workspace.
- Data: Added persisted pricing rules for A3, A4, Letter, Legal, and fallback paper sizes, split by B&W and color. Uploaded files are processed in memory, limited to 25 MB, and not retained.
- Verified: All five API tests, renderer typecheck/lint, web and desktop production builds, and the migration upgrade/downgrade/re-upgrade pass. Live viewport QA was unavailable because no in-app browser was connected.
- Next steps: Attach analysis snapshots/files to job orders, define ready-to-print export, and add OCR/AI recommendations only when those workflows are specified.

## 2026-08-24 (product-aware document pricing + Configuration page)

- Summary: Document analyzer pricing can now vary per product, and a new Configuration page consolidates catalog-wide configuration that was previously split across Services and Settings.
- Completed:
  - Added `ProductDocumentRate`, a per-product override of one global `DocumentPricingRule`, following the same global-library-plus-product-override shape as `Variant`/`ProductVariant`. Product create/edit forms gained a "Document pricing overrides" grid (`ProductDocumentRateSelector`) alongside the existing pricing-variant selector.
  - The analyzer's rate resolution is now three-tier: product override → exact paper-size global rate → `Unknown` global fallback rate (previously only the latter two existed). `PricingBreakdownItem.usedFallback` became `rateSource: "product" | "paperSize" | "fallback"`.
  - Document Analyzer gained an optional "Reference product" picker; analyzing against a product echoes back which product priced the result (`pricingContext`) and tags overridden rows.
  - Added the Configuration page (`/configuration`), pinned in the nav next to Settings. It now owns Global variants (moved from `/product-catalog/variants` to `/configuration/variants`, old path redirects) and Document analyzer pricing (moved out of Settings, which now just links over).
- Data: New `product_document_rates` join table (product_id, pricing_rule_id, price_per_page), additive only — no backfill needed.
- Verified: Backend pytest (including a new product-override pricing test), `alembic upgrade head` against the real local app database, and the web production build (`tsc -b && vite build`) all pass.
- Next steps: none blocking; job-order attachment of analyzer results remains separately planned.

## 2026-08-24 (paper stock, print type, and reference pricing)

- Summary: Document pricing is now tied to real inventory paper stock instead of a free-standing size list, a product's document-rate override only ever covers its own print type, and `Product.base_price` is gone — a product's price is computed from its document-pricing rate instead of a separate manually-typed number.
- Completed:
  - Added `InventoryItem.paper_size` (A4/Letter/Legal only; at most one active item per size). `DocumentPricingRule` now has an `inventory_item_id` FK instead of a free `paper_size` string; A3 and the `Unknown` fallback tier are gone entirely (the shop doesn't stock A3, and Unknown wasn't a real size). `ensure_defaults` creates rules dynamically from whichever paper items the owner has actually tagged, starting at ₱0.
  - `ProductDocumentRateSelector` dropped its Colored/B&W column split — a product's override list now only ever shows rates for its own required Print Type; the API rejects a mismatched override with 422.
  - Removed `Product.base_price`. Added `services/api/app/services/product_pricing.py::reference_price_per_page`: a product's price is the Letter-size rate for its own print type (its own override, else the global rate, else ₱0) plus variant adjustments — exposed as `ProductRead.pricePerPage`, used by job-order line pricing, the product list, and job-order product cards.
  - Product and Inventory forms updated accordingly: Product forms show a live computed "Reference price / page" instead of an editable base-price field; the Inventory item form gained an optional Paper size dropdown; Configuration's pricing grid shows an honest empty state until a paper item is tagged.
- Data: New migration (`e2c7a1f4b6d8`) adds `inventory_items.paper_size`, rebuilds `document_pricing_rules` on the new FK shape, and drops `products.base_price`. No job orders existed yet and no rate had been customized away from its seeded default, so nothing of value needed preserving; verified upgrade → downgrade → upgrade against both a scratch DB and the real local app database.
- Verified: Backend pytest (rewrote the job-order and document-analyzer tests to tag inventory items and seed rates instead of setting a flat base price), `tsc -b`, `vite build`, and `eslint` all pass.
- Next steps: none blocking; a per-order paper-size picker (so a product's price could use a size other than Letter per job order) remains a future option, not required by anything built so far.

## 2026-08-24 (inventory material deletion)

- Summary: Materials can now be deleted from Inventory, guarded against destroying linked or independently-recorded data, with a real confirmation modal instead of `window.confirm`.
- Completed:
  - Added `DELETE /inventory-items/{id}` (`services/api/app/routers/inventory.py`): 404 if missing, 409 if any product still has it assigned, 409 if it's used in a job-order material plan or a document-pricing rule. Refined the same day: the item's own stock-movement history no longer blocks deletion (it's deleted along with the item via the existing cascade) — the owner hit this on a test material with only adjustment history and no real dependency, so only records that live independently of the item (a product, a job order, a pricing rule) still block it.
  - Added `DeleteInventoryItemModal` (`apps/web/src/pages/inventory/DeleteInventoryItemModal.tsx`), following the same `Modal` + busy/error wiring as `StockAdjustmentModal`, warning that deletion also removes the item's movement history. `InventoryPage` gained a Delete button per row, proactively disabled with a tooltip when `linkedProductCount > 0` (matching the existing Variant-removal pattern), opening the modal for everything else.
- Verified: Backend tests cover blocked-by-assignment, blocked-by-pricing-rule, a movement-history item deleting successfully once unlinked, a never-used item deleting successfully, and 404 on a missing id — plus the full existing suite; `tsc -b`, `eslint`, and `vite build` all pass.
- Next steps: none blocking.

## 2026-08-27 (material-aware product and job pricing)

- Summary: Fixed configured A4/Legal product prices and job-order totals that previously used a hard-coded Letter rate.
- Completed: Product reference prices now use assigned paper-material rates; job lines use the exact selected paper material on the client and server, retain variant/page/copy calculation, and reject ambiguous multi-paper lines.
- Verified: All 7 API tests pass, including the selected-A4 regression; renderer and desktop typechecks, renderer lint, and the web production build pass.

## 2026-08-27 (consolidated product material setup)

- Summary: Removed the redundant second paper-material selection from product forms.
- Completed: Paper assignment and global/custom pricing now share one row; non-paper supplies remain under Other materials; a responsive read-only Assigned materials summary appears on the right at wide widths and below the controls on narrow screens.
- Verified: All 7 API tests, renderer/desktop typechecks, renderer lint, and the web production build pass. Live browser QA was unavailable because no browser window was connected.

## 2026-08-27 (configured paper size in job creation)

- Summary: Aligned job-order creation with product paper and pricing configuration.
- Completed: The owner now selects one configured paper size per product line; its exact rate updates the line and order totals, and its linked Inventory material and planned sheet quantity are added automatically. Only non-paper supplies remain optional checkboxes. The API rejects missing, ambiguous, or unpriced paper selections.
- Verified: All 7 API tests, renderer/desktop typechecks, renderer lint, and the web production build pass.

## 2026-08-27 (simplified job production inputs)

- Summary: Removed the redundant Print sides input from New Job Order.
- Completed: Job creation now uses product, configured paper size, variant, pages, and copies. Automatic paper planning uses pages × copies; the legacy API/database field remains at its default for compatibility.
- Verified: All 7 API tests, renderer/desktop typechecks, renderer lint, and the web production build pass.

## 2026-08-27 (document analyzer split preview)

- Summary: Rebuilt the analyzed-document view as a source-and-results preflight desk.
- Completed: Added a left local preview for PDF and browser-supported images, a format-aware fallback for Office/TIFF files, and a right analysis pane containing metrics, pricing, warnings, privacy status, and actions. The panes stack preview-first on narrow screens.
- Verified: All 7 API tests, renderer/desktop typechecks, renderer lint, and the web production build pass. Live visual QA was unavailable because no browser window was connected.

## 2026-08-27 (interactive PDF preview engine)

- Summary: Replaced the unreliable PDF iframe with a dedicated local viewer.
- Completed: Added lazy-loaded PDF.js rendering, previous/next and direct page navigation, zoom, fit-to-width, rotation, original-file download, keyboard controls, high-DPI canvas output, and loading/retry/error states.
- Verified: All 7 API tests, renderer/desktop typechecks, renderer lint, and the web production build pass. The PDF engine and worker build as separate lazy-loaded assets.

## 2026-08-27 (full-pane continuous PDF workspace)

- Summary: Expanded analyzed documents into a full app-pane workspace and removed PDF pagination.
- Completed: The analyzed state now suppresses the standard page header, removes the global content-width cap, fills the available desktop height, and presents every PDF page in one scrollable stack. Nearby pages render ahead of the viewport while zoom, fit, rotation, and download remain available.
- Verified: All 7 API tests, renderer/desktop typechecks, renderer lint, and the web production build pass.

## 2026-08-27 (analyzer pricing and environment cleanup)

- Summary: Aligned document estimates with configured product pricing and simplified deployment/navigation/profile configuration.
- Completed: Product-selected analysis now uses assigned paper rules and the product print type; PDF extraction failures are surfaced; stage and SQLite path come from `.env`; Production was removed; owner name is editable and shown in the sidebar.
- Verified: All 9 API tests, renderer/desktop typechecks, renderer lint, web production build, and a clean full Alembic migration pass.

## 2026-08-27 (sidebar connection copy)

- Summary: Shortened the online sidebar status from “Backend connected” to “Connected.”
- Verified: Renderer typecheck and lint pass.

## 2026-08-27 (coverage-aware analyzer pricing)

- Summary: Replaced the unused quotation UI with a more capable product-based document estimate.
- Completed: Added rendered PDF color/ink measurement, conservative Office estimates, required product and optional variant selection, and a transparent base/ink/color/variant pricing breakdown. Removed all active Quotations surfaces without deleting legacy backend data.
- Verified: All 9 API tests, renderer/desktop typechecks, renderer lint, and the web production build pass.

## 2026-08-27 (application icon branding)

- Summary: Applied The Paper Club logo mark to browser and native application surfaces.
- Completed: Added the favicon, Electron window/taskbar icon, development macOS dock icon, and packaged Windows/macOS icon configuration.
- Verified: Renderer and desktop typechecks, renderer lint, and the web production build pass.

## 2026-08-27 (Windows Canon and vendor-neutral printer connection)

- Summary: Made Print Center ready to connect the owner's Canon through Windows without coupling the app to Canon.
- Completed: Implemented Windows queue discovery through `Win32_Printer`, added native Printers & scanners access, a Canon-first three-step setup guide, cross-brand queue copy, and stale-queue offline reconciliation.
- Verified: All 11 API tests, renderer/desktop typechecks, renderer lint, and the web production build pass. Physical Canon validation remains for the Windows workstation.

## 2026-08-27 (printer host OS detection)

- Summary: Print Center now identifies which operating system controls its printer adapter.
- Completed: Added automatic Windows/macOS/Linux detection, the optional `PRINT_MS_PRINTER_PLATFORM` override, a platform-information API, and visible loading/ready/error/retry states in Print Center.
- Verified: All 12 API tests, renderer/desktop typechecks, renderer lint, and the web production build pass.

## Template

### YYYY-MM-DD

- Summary:
- Completed:
- In progress:
- Next steps:
## 2026-08-28 (confirmed transaction workflow)

- Summary: Connected customer-file intake, product selection, document analysis, owner pricing approval, job creation, durable file staging, and Print Center handoff.
- Completed: New Job Order is now a validation-aware transaction wizard. Analysis saves nothing; owner confirmation revalidates pricing server-side, records suggested/final pricing, creates the paper plan and job file, then opens the staged Print Center context. Job details expose the approved file and pricing decision.
- Verified: All 13 API tests, Alembic head inspection, renderer/desktop typechecks, renderer lint, web production build, and `git diff --check` pass.
## 2026-08-28 (overview quick transaction action)

- Summary: Moved Quick Actions to the top of Overview and made New Job Order the dominant one-click workflow.
- Completed: Added responsive primary/secondary action tiles and URL-driven opening of the existing job-order transaction modal, including clean query removal when cancelled.
- Verified: Renderer/desktop typechecks, renderer lint, web production build, and `git diff --check` pass.
## 2026-08-28 (stage-separated SQLite environments)

- Summary: Added independently configurable development, test, and production SQLite locations.
- Completed: Added three stage-specific `.env` variables with isolated defaults, retained the legacy active-stage override, exposed every resolved path/source through health diagnostics, and added a responsive Runtime Environments matrix to Settings.
- Verified: Configuration tests cover explicit stage paths, compatibility override, and isolated defaults; the full API and renderer verification suites pass.
## 2026-08-28 (payment-to-completion production workflow)

- Summary: Completed the operational path from Pending Payment through printed and completed job order.
- Completed: Added verified partial/full payments, guarded status transitions, a responsive production stepper and timeline, vendor-neutral confirmed OS submission, printer/file/settings selection, durable success/failure attempts, and manual quality-check/ready/completed actions.
- Verified: Full fresh Alembic migration, 16 API tests including failed/retried print submission and every lifecycle gate, renderer/desktop typechecks, renderer lint, web production build, and `git diff --check` pass. Physical Canon validation remains open on the Windows workstation.

## 2026-08-28 (Windows PDF print submission repair)

- Summary: Removed the Windows PDF file-association dependency that prevented the Canon queue from receiving jobs.
- Completed: Printing-MS now renders PDF/image pages locally at 300 DPI, applies color/grayscale, copies, orientation, and supported paper size, and draws them directly to the selected Windows driver queue. Print failures are concise and Print History status labels no longer collapse vertically.
- Verified: Windows adapter tests cover local PDF rendering, queue arguments, and unsupported-format guidance. Full automated verification passes; physical Canon output revalidation remains open on the Windows workstation.

## 2026-08-28 (automatic print profile and inventory consumption)

- Summary: Connected analyzer output through print submission and inventory usage without repeated operator entry.
- Completed: Confirmed files retain detected pages, paper, orientation, color separation, coverage, timing, and confidence. Print Center shows the derived profile read-only; the API derives its settings independently. Successful queue submission consumes every remaining planned material, blocks on low stock, records job-linked ledger entries, and leaves stock unchanged on failure.
- Verified: Automated workflow coverage includes persisted analysis metadata, ignored legacy setting hints, low-stock blocking, no deduction on printer failure, one-time deduction on success, and ledger balances.

## 2026-08-29 (all-inclusive B&W analyzer pricing)

- Summary: Removed duplicate ink and detected-color charges from B&W document estimates.
- Completed: B&W products now price from the exact configured paper-size rate × pages, with copies applied by the transaction and only explicit variants added. Analyzer and transaction breakdowns identify the B&W base as including paper and ink.
- Verified: A4, Letter, and Legal coverage proves high source ink/color cannot alter B&W pricing. All 20 API tests, web/desktop typechecks, lint, and production builds pass.

## 2026-08-29 (app-managed print types)

- Summary: Added Semi-colored and removed hardcoded print-type limits.
- Completed: Migrated products and paper pricing to a shared print-type catalog; added automatic rate generation, dynamic pricing/product controls, in-app type creation, analyzer pricing behavior, and authoritative printer color-mode mapping.
- Verified: Fresh SQLite migration reaches the single Alembic head; all 20 API tests, web/desktop typechecks and builds, and renderer lint pass.

## 2026-08-29 (print-type modal layout)

- Summary: Corrected the New print type modal’s spacing and containment.
- Completed: Added a padded scrollable form body, compact component width, distinct footer actions, clearer field hierarchy, disabled submission state, and narrow-screen insets.
- Verified: Renderer lint, TypeScript checks, production build, and diff validation pass.

## 2026-08-29 (advisory document-size workflow)

- Summary: Separated detected document size from the owner's intended print paper.
- Completed: Added a configured paper selector before analysis, changed the review to show non-blocking best-fit guidance, and made the selection authoritative for pricing, material planning, stock deduction, and printer media while retaining detected size as file evidence.
- Verified: All 20 API tests, web/desktop typechecks and builds, and renderer lint pass. Regression coverage confirms an A4 source can proceed on owner-selected Letter paper end to end.

## 2026-08-29 (Windows driver-aligned Print Center)

- Summary: Added auditable Windows-style print settings and clarified the printer hierarchy.
- Completed: Print attempts now apply and retain orientation, scaling, quality, borderless behavior, and collation. Print Center highlights only the OS default printer, groups selectable alternatives under Others, and opens the selected Windows driver's preferences for Canon-specific media and tray controls.
- Verified: Fresh Alembic migration, all 20 API tests, renderer/desktop typechecks and builds, renderer lint, and diff validation pass.

## 2026-08-29 (single-page job-order workflow)

- Summary: Consolidated the complete transaction lifecycle into the job-order workspace.
- Completed: Job creation now opens the new job rather than Print Center. Payment, queueing, print setup, print completion, quality approval, ready, and final completion use focused modals. Replaced seven detail cards with the active lifecycle, two essential summaries, and one collapsed audit pane.
- Verified: All 20 API tests, renderer/desktop typechecks and builds, renderer lint, route scan, and diff validation pass.

## 2026-08-29 (source-aware automatic print profile)

- Summary: Decoupled commercial print types from physical document rendering.
- Completed: Printing now derives color preservation from analyzed source content, uses automatic per-page orientation and driver printable margins, leaves quality at the installed driver default, and keeps borderless as a deliberate media-dependent override. Print-type creation now describes pricing behavior only.
- Verified: All 21 API tests, web/desktop typechecks and production builds, renderer lint, responsive layout rules, and diff validation pass. Physical Canon output still requires the Windows workstation.

## 2026-08-29 (Canon-aligned source margins)

- Summary: Removed the duplicate Windows hard-margin inset and made source-preserving scaling the default.
- Completed: Windows now draws against the driver-reported printable rectangle once. Automatic scaling keeps the document's original dimensions and margins when possible and shrinks only when hardware bounds require it; explicit Fit, Fill, Actual size, and borderless controls remain available.
- Verified: All 22 API tests, web/desktop typechecks and production builds, renderer lint, and diff validation pass. Physical Canon comparison remains open on the Windows workstation.

## 2026-08-29 (external Windows print monitoring)

- Summary: Added durable observation of Canon PRINT and other Windows-spooled jobs.
- Completed: An application-lifetime PowerShell monitor reads `Win32_PrintJob`, persists status/page/owner/document metadata, marks departed jobs as released, links internally submitted attempts by tracking marker, and presents external records in a responsive External / Unlinked Print Center pane. Direct printer-panel and direct mobile/cloud activity is explicitly outside the Windows audit boundary.
- Verified: Fresh migration reaches the single Alembic head; all 25 API tests, web/desktop typechecks and production builds, renderer lint, and diff validation pass. Coverage validates external persistence, status updates, release handling, internal linking, API serialization, and monitor script behavior.

## 2026-08-29 (external-print intake and transaction preview)

- Summary: Turned unlinked Windows spooler observations into an owner-reviewed job-order intake path.
- Completed: New external jobs raise a non-blocking app prompt, may be dismissed or opened as a job, and remain visible in Print Center with their review/link state. Job creation now uses File → Print setup → Preview & price, reuses the continuous interactive PDF viewer beside analysis/pricing, and links the external observation only after final transaction approval.
- Verified: Fresh SQLite migration, all 25 API tests, web/desktop typechecks and builds, renderer lint, and diff validation pass.

## 2026-08-29 (supervised manual duplex printing)

- Summary: Added a resumable two-pass production workflow for Back-to-Back jobs on Windows.
- Completed: Variants can explicitly require supervised duplex; existing Back-to-Back variants and job snapshots migrate automatically. The job modal prints fronts, pauses with Canon-aligned stack instructions, requires reinsertion confirmation, then prints backs. Passes retain identical printer/file/settings, odd-page documents consume a blank back feed to preserve alignment, paper planning uses physical sheets, and inventory deducts only after the back pass succeeds.
- Verified: Fresh SQLite migration reaches the single head; all 26 API tests, renderer/desktop typechecks and production builds, renderer lint, physical pass-order unit coverage, and diff validation pass.

## 2026-08-29 (scalable job references and global print activity)

- Summary: Made high-volume job references durable and exposed concurrent queue activity throughout the app.
- Completed: New jobs use a transactional 10-digit sequence, while their UUID identity remains unchanged. A floating shell tracker polls every queued/printing job, shows spooler page progress, expands on hover, opens an accessible activity modal, and links directly to each job. Windows release, pause, and error events become persistent owner-attention states until the workflow advances.
- Verified: Fresh and existing SQLite migrations, all 28 API tests, renderer/desktop typechecks and production builds, renderer lint, million-scale sequence and print-activity endpoint coverage, and diff validation pass.

## 2026-08-29 (job workflow action contrast)

- Summary: Restored readable action labels in the job-order command panel.
- Completed: Scoped the panel's eyebrow styling away from nested button labels, reinforced inherited button foregrounds, and removed the route-local printer-busy block so another job can still enter the OS queue.
- Verified: Renderer typecheck, lint, production build, and diff validation pass.

## 2026-08-29 (human-readable job names)

- Summary: Added owner-friendly names without weakening durable job references.
- Completed: Job creation prefills a required editable name from the uploaded filename. Job lists, workspaces, print setup, workflow modals, and the global print tracker now lead with that name and retain the 10-digit job number as secondary audit metadata. Existing jobs are backfilled from their retained filename when available.
- Verified: Single-head fresh/existing SQLite migrations, all 28 API tests, renderer/desktop typechecks and production builds, renderer lint, and diff validation pass.

## 2026-08-29 (photocopy service workflow)

- Summary: Added category-driven services and a no-file photocopy transaction workflow.
- Completed: Added Printing, Photocopy, and Custom categories; service-first job intake; independent B&W photocopy paper rates; live page/copy/duplex pricing; immediate physical-sheet deduction; and a photocopy-specific Ready → Paid → Completed workspace.
- Verified: Fresh and legacy-data migrations reach the single Alembic head; all 28 API tests, renderer/desktop typechecks and builds, renderer lint, and diff validation pass.

## 2026-08-29 (scan transaction workflow)

- Summary: Expanded Photocopy into the combined Scan or Photocopy service.
- Completed: Added operation-specific products, standalone scan pricing, no-inventory scan validation, scanner-output attachment/page verification, retained softcopy download, and Scan-specific Ready → Paid → Completed job presentation.
- Verified: Fresh and legacy-photocopy migrations reach the single head; all 28 API tests, renderer/desktop typechecks and builds, renderer lint, and diff validation pass.

## 2026-08-29 (direct Windows scan acquisition)

- Summary: Replaced upload-first Scan intake with scanner-first acquisition and retained in-app preview.
- Completed: Added a narrow Electron-to-WIA bridge, installed-driver acquisition UI, repeated page capture, automatic page counting/pricing, multi-page PDF composition, recovery import, pre-confirmation previews, and an authenticated saved-softcopy viewer.
- Verified: All 28 API tests and renderer/desktop typechecks and lint pass. Physical Canon WIA validation remains required on Windows.

## 2026-08-29 (scanner readiness preflight)

- Summary: Replaced raw WIA failures with actionable device and original-placement validation.
- Completed: Added scanner discovery, selectable flatbed/ADF sources, online/capability/readiness checks, jam and cover blocking, manual placement confirmation for drivers without paper sensing, and concise mappings for standard WIA acquisition errors.
- Verified: Renderer/desktop typechecks, lint, production builds, API regression tests, and diff validation pass. Physical Canon status-flag validation remains open.

## 2026-08-29 (Windows Canon scanner deployment prerequisite)

- Summary: Recorded the driver installation that resolved Canon scanner discovery on the Windows workstation.
- Completed: Added a Windows installation guide covering the model-specific IJPAT/full MP/WIA prerequisite, Canon network-scanner selection, Windows Scan and Printing-MS acceptance checks, troubleshooting, and requirements for a future production installer.
- Verified: The owner confirmed that installing the IJPAT package made the connected scanner discoverable. Remaining physical acquisition and readiness scenarios stay open for validation.

## 2026-08-30 (scan preview and automatic source)

- Summary: Repaired WIA page previews and removed manual source selection from Scan intake.
- Completed: WIA capture now converts driver-native output to validated PNG before IPC. Automatic acquisition prefers a driver-reported loaded feeder or flatbed and otherwise lets the Canon/Windows dialog choose; offline, jam, cover, cancellation, and acquisition errors remain actionable. Broken image icons now degrade to an explicit preview-unavailable tile.
- Verified: Renderer and desktop typechecks/builds, renderer lint, all 28 API tests, and diff validation pass. Physical feeder/flatbed selection still requires confirmation on the Windows Canon workstation.

## 2026-08-30 (in-app scanner configuration)

- Summary: Removed the redundant Windows WIA settings popup from normal Scan jobs.
- Completed: Added in-app content mode, 150/300/600 DPI, and Automatic/A4/Letter/Legal/4×6/5×7/8×10 capture-size controls. The desktop bridge validates the profile, applies standard WIA properties, transfers directly as BMP without `ShowSelectItems`, normalizes to PNG, and returns unsupported combinations as actionable inline errors.
- Verified: Renderer/desktop typechecks and production builds, renderer lint, all 28 API tests, and diff validation pass. Direct WIA transfer and each Canon paper/source combination still require physical Windows validation.

## 2026-08-30 (Canon scan activation and placement gate)

- Summary: Repaired the no-op Canon scan action and prevented unconfirmed acquisition.
- Completed: Replaced unreliable direct item transfer with WIA's transfer/progress operation while continuing to skip its settings selector. Automatic source selection remains; detected feeder paper proceeds automatically, while flatbed and non-sensing sources require owner confirmation enforced by the desktop bridge.
- Verified: Renderer/desktop typechecks and production builds, renderer lint, all 28 API tests, and diff validation pass. Physical acquisition and status sensing remain to be validated on the Windows Canon workstation.

## 2026-08-30 (printing navigation labels)

- Summary: Clarified the two primary printing areas in the application shell.
- Completed: Renamed Job Orders to Printing Job Orders and Services to Printing Services across navigation, list headings, breadcrumbs, and service workspaces.
- Verified: Renderer typecheck, lint, production build, and diff validation pass.

## 2026-08-30 (feeder-aware scanning and visible outcomes)

- Summary: Corrected Canon source selection, recovered unsupported B&W scans, and made every scanner trigger observable.
- Completed: WIA 2.0 item-category discovery now selects the feeder/flatbed transfer item instead of item 1. Automatic mode honors the feeder-ready status bit and otherwise tries the feeder first, with explicit source overrides. Rejected B&W text intent retries as grayscale and reports the fallback. The modal retains check, progress, completion, cancellation, and error guidance.
- Verified: Renderer/desktop typechecks, production builds, renderer lint, all 28 API tests, and diff validation pass. Physical Canon source and B&W validation remains open.
