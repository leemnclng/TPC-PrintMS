{
  "name": "printing-ms",
  "private": true,
  "version": "0.1.0",
  "description": "The Paper Club — Printing-MS desktop application (Electron + React/TS renderer, FastAPI backend).",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n web,desktop -c blue,magenta \"npm run dev:web\" \"npm run dev:desktop:wait\"",
    "dev:web": "npm run dev --workspace apps/web",
    "dev:desktop:wait": "wait-on tcp:5173 && npm run dev:desktop",
    "dev:desktop": "npm run dev --workspace apps/desktop",
    "build:web": "npm run build --workspace apps/web",
    "build:desktop": "npm run build --workspace apps/desktop",
    "typecheck": "npm run typecheck --workspace apps/web && npm run typecheck --workspace apps/desktop",
    "lint": "npm run lint --workspace apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "wait-on": "^8.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
